export enum CheckboxVariant {
    Classic = "classic",
    Soft = "soft",
}

export enum CheckboxSize {
    Small = "small",
    Medium = "medium",
    Large = "large",
}

export enum CheckboxRadius {
    Small = "small",
    Large = "large",
}
