export enum TagVariant {
    Contained = "contained",
    Outlined = "outlined",
    Soft = "soft",
}

export enum TagColor {
    Primary = "primary",
    Secondary = "secondary",
    Accent = "accent",
}

export enum TagSize {
    Small = "small",
    Medium = "medium",
    Large = "large",
}

export enum TagRadius {
    Small = "small",
    Medium = "medium",
    Large = "large",
}